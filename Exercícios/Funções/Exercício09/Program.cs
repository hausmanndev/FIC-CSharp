//3. * *Função Recursiva para Fibonacci**
//    - Implemente uma função recursiva que recebe um número `n` e retorna o `n`ésimo termo da sequência de Fibonacci.

Console.Write("Digite um número: ");
int n = int.Parse(Console.ReadLine());

static int Fibonacci(int n)
{

    return Fibonacci(n);
}